

les coureurs partent de maniere decalée mais arrivent en meme temps, sur la meme ligne. il faut prévoir pour chaque participant de savoir a quelle course il participe

le format de l'heure, doit etre sans AM/PM. Utilise des heures de 0 a 24

je voudrais un theme clair

je voudrais un affichage un peu plus compact, seule la moitie de l'ecran est utilise

la liste des course dans le panel de gauche ne s'affiche pas correctement au niveau des couleurs. c'est une police claire sur fond clair, ce qui rend les choses illisibles. pareil pour le bouton "Reinitialiser toutes les courses"

when I run `npm run build` I get this error
```
error TS1484: 'KeyboardEvent' is a type and must be imported using a type-only import when 'verbatimModuleSyntax' is enabled
```

les boutons ne sont pas lisibles, il faudrait un fond plus foncé

change la couleur de fond du champ de saisie du numero de dossard : fond clair

ajoute un chrono temps réel pour la course sélectionnée

supprime la colonne `Temps (s)` du tableau de resultats, garde uniquement le temps au format hh:mm:ss

renomme la colonne bib en bib_number



annule le changement `colonne bib en bib_number`

lorsque l'arrivee d'un participant a deja ete saisie, il ne faut pas pouvoir a nouveau saisr l'arrivee pour le meme numero de dossard 